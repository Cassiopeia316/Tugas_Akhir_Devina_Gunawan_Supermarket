import React from 'react'
import { Route, Routes } from 'react-router-dom'

export const AdminEndpoint = () => {
    return (
        <Routes>
            <Route path="/" />
        </Routes>
    )
}
